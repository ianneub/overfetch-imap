# Test environment variables for BATS tests
# These values are used by test_helper.bash to set up test environments

# IMAP Server Configuration
MAIL_SERVER=imap.example.com
MAIL_PORT=993

# IMAP Credentials
USERNAME=testuser@example.com
PASSWORD=testpassword123

# Rails Action Mailbox Configuration
RAILS_MAIL_INBOUND_URL=https://example.com/rails/action_mailbox/postfix/inbound_emails
INGRESS_PASSWORD=ingress-secret

# Message Retention (empty = delete, "keep" = retain on server)
KEEP=
